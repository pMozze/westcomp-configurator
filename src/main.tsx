import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.tsx';
import './globals.css';

ReactDOM.createRoot(document.getElementById('westcomp-react-configurator')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
